import {loadGoals} from './dataLoader.js';import {populateFilterOptions,getFilterState,applyFilters} from './filters.js';import {summarizeGoals} from './stats.js';import {renderScatter,renderHeatmap} from './plots.js';
let all=[],view='scatter'; const plot=document.getElementById('plot');
function debounce(fn,ms){let t;return(...a)=>{clearTimeout(t);t=setTimeout(()=>fn(...a),ms);};}
function render(){const filtered=applyFilters(all,getFilterState());(view==='scatter'?renderScatter:renderHeatmap)(filtered,plot);const s=summarizeGoals(filtered);document.getElementById('summary').innerHTML=[['Total goals',s.total],['Mean minute',s.mean.toFixed(1)],['Median minute',s.median.toFixed(1)],['90+ %',s.pct90.toFixed(1)+'%'],['75+ %',s.pct75.toFixed(1)+'%'],['Earliest',s.earliest],['Latest',s.latest]].map(([k,v])=>`<div class='card p-3'><div class='text-xs text-slate-400'>${k}</div><div class='text-xl font-semibold'>${v}</div></div>`).join('');document.getElementById('interpretation').textContent=`In the current filter, ${s.pct75.toFixed(1)}% of goals happen from the 75th minute onward.`;}
loadGoals().then(d=>{all=d;populateFilterOptions(all);document.getElementById('controls').addEventListener('input',render);document.getElementById('viewToggle').addEventListener('click',e=>{if(e.target.dataset.view){view=e.target.dataset.view;document.querySelectorAll('.toggle').forEach(b=>b.classList.toggle('active',b.dataset.view===view));render();}});window.addEventListener('resize',debounce(render,150));render();});
